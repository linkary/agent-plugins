# Quick Task 260425-qo9: Clean duplicate credential notice and remote prefix in skills update progress

**Date:** 2026-04-25
**Status:** In progress

## Goal

Clean up `ap skills update` output:
- Do not print a separate `Credentials required for repo` line before the username prompt.
- Strip `remote:` prefixes from progress failure summaries.

## Tasks

1. Remove the duplicate interactive credential notice.
   - Files: `src/commands/skills/update.ts`, `tests/skills-update.test.ts`
   - Verify: credential prompt still contains the repo URL.

2. Normalize git error summaries.
   - Files: `src/commands/skills/update.ts`
   - Verify: `remote: Invalid...` becomes `Invalid...` in progress and final error text.

3. Run available checks.
   - Verify: `git diff --check`; `bun test tests/skills-update.test.ts` if Bun is available.
