# Quick Task 260425-q8r: Keep failed repo entries visible in parallel skills update progress

**Date:** 2026-04-25
**Status:** In progress

## Goal

Make `ap skills update` parallel progress keep failed repo entries visible while other repos continue, and include a short error summary in the failed row.

## Tasks

1. Capture git failure stderr for clone/fetch/checkout during repo update checks.
   - Files: `src/commands/skills/update.ts`, `tests/skills-update.test.ts`
   - Verify: existing mocked clone behavior still works.

2. Change progress rendering to show retained failure entries separately from active worker slots.
   - Files: `src/commands/skills/update.ts`
   - Verify: failed rows use `fail [error] repo` and remain visible while active rows continue.

3. Run available checks and document unavailable test tooling.
   - Verify: `git diff --check`; `bun test tests/skills-update.test.ts` if Bun is available.
