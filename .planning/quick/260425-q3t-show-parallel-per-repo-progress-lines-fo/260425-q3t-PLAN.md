# Quick Task 260425-q3t: Show parallel per-repo progress lines for ap skills update

**Date:** 2026-04-25
**Status:** In progress

## Goal

Change `ap skills update` progress from one aggregate progress bar to parallel per-repo progress lines similar to `uv`, where concurrent repo checks are visible at the same time.

## Tasks

1. Let bounded parallel mapping expose the worker slot index to callers.
   - Files: `src/commands/skills/update.ts`
   - Verify: existing repo result ordering is preserved.

2. Replace the single-line progress renderer with a TTY-only multi-line renderer.
   - Files: `src/commands/skills/update.ts`
   - Verify: each concurrent slot renders its current repo and final status; non-TTY output stays clean.

3. Run available checks and document unavailable test tooling.
   - Verify: `git diff --check`; `bun test tests/skills-update.test.ts` if Bun is available.
