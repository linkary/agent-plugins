# Quick Task 260426-c6l: Implement shared same-name conflict handling

## Goal

Apply one source-aware same-name conflict policy across skills, agents, commands, and rules while preserving MCP's existing overwrite behavior.

## Scope

- Add shared source identity and conflict classification helpers.
- Wire source-aware conflict handling into artifact add flows.
- Ensure replace/alias/skip semantics update central records and git repo tracking correctly.
- Filter stale repo tracking during update so old duplicate repo entries are not scanned.
- Record collected sources consistently when collect flows overwrite central items.
- Add focused unit coverage for the shared helper.

## Verification

- `git diff --check`
- Targeted Bun tests and build if `bun` is available in the environment.
