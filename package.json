{
  "name": "agent-plugins",
  "version": "0.1.0",
  "description": "LLM skills manager and sync tool (ap)",
  "type": "module",
  "main": "dist/cli.mjs",
  "bin": {
    "apm": "bin/ap.js",
    "agent-plugins": "bin/ap.js",
    "ap": "bin/ap-dev.js"
  },
  "scripts": {
    "build": "bun build src/cli.ts --target node --format esm --outfile dist/cli.mjs --external @shikijs/cli --external shiki",
    "dev": "bun run src/cli.ts",
    "prepack": "bun run build",
    "test": "bun test"
  },
  "files": [
    "dist",
    "bin"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "@types/node": "^25.2.0",
    "@types/react": "^19.1.0"
  },
  "dependencies": {
    "@shikijs/cli": "^3.22.0",
    "ink": "^6.6.0",
    "react": "^19.1.0",
    "react-devtools-core": "^6.1.2"
  }
}
